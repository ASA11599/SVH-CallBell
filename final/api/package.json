{
  "name": "svh-callbell-api",
  "version": "1.0.0",
  "description": "An API to store and retrieve calls for the SVH volunteer call bell system",
  "main": "api.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "sqlite-sync": "^0.3.9"
  }
}
